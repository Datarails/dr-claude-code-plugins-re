{
  "mcpServers": {
    "datarails-finance-os": {
      "command": "uv",
      "args": ["--directory", "./mcp-server", "run", "datarails-mcp", "serve"],
      "env": {
        "DATARAILS_CONFIG_DIR": "./config"
      }
    }
  }
}
